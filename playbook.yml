---
- name: Deploy
  hosts: all

  vars_files: [vars.yml]

  roles:
    - role: base
      tags: base

    - role: nginx
      tags: nginx

    - role: transmission
      tags: transmission

    - role: mariadb
      tags: mariadb

    - role: gitea
      tags: gitea

  tasks:
    - name: Print link to homepage
      debug:
        msg: ' Homepage: http://{{ ansible_host }} '
