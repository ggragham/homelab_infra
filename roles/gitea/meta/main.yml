---
galaxy_info:
  author: Grell Gragham
  description: Install and config Gitea git server.

  license: license (GPL-3.0-only)

  min_ansible_version: '2.1'

  platforms:
    - name: Debian
      versions:
        - all

dependencies:
  - role: mariadb
  - role: nginx
